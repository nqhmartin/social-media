import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

interface Props {
  navigation: any;
}

const TabFollowing: React.FC<Props> = ({navigation}) => {
  return (
    <View>
      <Text>TabFollowing</Text>
    </View>
  );
};

export default TabFollowing;

const styles = StyleSheet.create({});
